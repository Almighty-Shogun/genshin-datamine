import type { ObfuscatedValue } from './common';

export type ActivityGreatFestivalV2Target = {
    configID: number;
    groupId: number;
    targetId: number;
    targetType: string;
} & ObfuscatedValue;
